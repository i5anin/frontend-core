---
tags:
  - веб-разработка
  - vue
  - css
  - html
  - typescript
created: 2024-10-14
date: 2024-10-14
---
работает

```html

<template>

<DataTable

v-if="dataLoaded"

:data="formattedData"

class="display"

:options="dataTableOptions"

>

<thead>

<tr>

<th v-for="(field, index) in headers" :key="index">{{ field.title }}</th>

</tr>

</thead>

</DataTable>

</template>

<script setup lang="ts">

import {defineProps, onMounted, ref} from 'vue'

import DataTable from 'datatables.net-vue3'

import DataTablesCore from 'datatables.net'

import 'datatables.net-bs5';

import 'datatables.net-bs5/css/dataTables.bootstrap5.min.css';

DataTable.use(DataTablesCore)

const props = defineProps<{

info: object;

fields: { name: string; title: string }[];

dataTable: any[];

}>()

const headers = ref([])

const formattedData = ref([])

const dataLoaded = ref(false) // Состояние для контроля загрузки данных

const updateTableData = () => {

headers.value = props.fields.map(field => ({name: field.name, title: field.title}))

formattedData.value = props.dataTable.map(row => headers.value.map(header => row[header.name] || ''))

console.log('Обновлены заголовки:', headers.value)

console.log('Обновлены отформатированные данные:', formattedData.value)

// Устанавливаем флаг после форматирования данных

dataLoaded.value = true

}

const dataTableOptions = ref({

processing: true,

serverSide: true,

columns: props.fields.map(field => ({

data: field.name,

title: field.title

})),

language: {url: 'Russian.json'}

})

onMounted(() => {

updateTableData()

})

</script>

```

как сделать на сервисайд что бы работало?
```html
<template>
<DataTable
v-if="dataLoaded"
:data="formattedData"
class="display"
:options="dataTableOptions"
>
<thead>
<tr>
<th v-for="(field, index) in headers" :key="index">{{ field.title }}</th>
</tr>
</thead>
</DataTable>
</template>
<script setup lang="ts">
import {defineProps, onMounted, ref} from 'vue'

import DataTable from 'datatables.net-vue3'

import DataTablesCore from 'datatables.net'

import 'datatables.net-bs5';

import 'datatables.net-bs5/css/dataTables.bootstrap5.min.css';

DataTable.use(DataTablesCore)

const props = defineProps<{

info: object;

fields: { name: string; title: string }[];

dataTable: any[];

}>()

const headers = ref([])

const formattedData = ref([])

const dataLoaded = ref(false) // Состояние для контроля загрузки данных

const updateTableData = () => {

headers.value = props.fields.map(field => ({name: field.name, title: field.title}));

formattedData.value = props.dataTable.map(row => {

return headers.value.map(header => {

return row.hasOwnProperty(header.name) ? row[header.name] : ''; // Значение по умолчанию для отсутствующих полей

});

});

console.log('Обновлены заголовки:', headers.value);

console.log('Обновлены отформатированные данные:', formattedData.value);

// Устанавливаем флаг после форматирования данных

dataLoaded.value = true;

};

const dataTableOptions = ref({

processing: true,

serverSide: true,

columns: props.fields.map(field => ({

data: field.name,

title: field.title

})),

language: {url: 'Russian.json'}

})

onMounted(() => {

updateTableData()

})

</script>
```

---

## Связанные

- [[Изучение]]
- [[API (2)]]
- [[API]]
- [[App.vue (2)]]
- [[App.vue]]
- [[array (2)]]
