---
tags:
  - веб-разработка
  - vue
  - javascript
  - api
created: 2024-10-13
date: 2024-10-13
---
**`pop()`** — это метод массивов в [JavaScript](./JavaScript), который удаляет **последний элемент** массива. Он изменяет исходный массив и возвращает удалённый элемент. Если массив пуст, метод возвращает `undefined`.

**Синтаксис:**

```javascript
array.pop()
```

- Метод **не принимает аргументов**.
- Возвращает последний элемент массива или `undefined`, если массив пуст.

**Пример использования:**

```javascript
let fruits = ['яблоко', 'банан', 'апельсин'];
let lastFruit = fruits.pop();

console.log(fruits);     // ['яблоко', 'банан']
console.log(lastFruit);  // 'апельсин'
```

В этом примере последний элемент массива `'апельсин'` был удалён и возвращён переменной `lastFruit`. Массив `fruits` изменился — остались только два элемента.

**Пример с пустым массивом:**

```javascript
let emptyArray = [];
let result = emptyArray.pop();

console.log(result);  // undefined
```

Если вызвать `pop()` для пустого массива, метод вернёт `undefined`, так как удалять нечего.

**Основные моменты:**
- **`pop()`** удаляет последний элемент массива.
- Возвращает удалённый элемент.
- Изменяет исходный массив.
- Если массив пуст, возвращает `undefined`.

---

## Связанные

- [[Изучение]]
- [[API (2)]]
- [[API]]
- [[App.vue (2)]]
- [[App.vue]]
- [[array (2)]]
